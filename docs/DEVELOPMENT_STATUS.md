# 開発状況

## 現在の開発段階

基本操作、インタラクション、状態保持、2 マップ間の遷移、共通 UI を備えた**探索型謎解きゲームの初期プロトタイプ段階**である。第一段階の工程整理を実施し、メインシーン、Player、GameUI、インタラクション、開発用マップを用途別ディレクトリへ分離した。現在の `prototype_room_a.tscn`／`prototype_room_b.tscn` は機能検証用の簡易マップであり、序章「休眠ポッド室」の完成仕様は未実装である。

## 完了している機能

- DotGothic16 を使用した、1280×720 基準のタイトル画面第一版
- タイトル画面の「はじめる」「設定」「終了」と、キーボード／マウスによるフォーカス操作
- 「はじめる」による Demo 状態の初期化と、開発用 `prototype_room_a.tscn` への一時的な遷移
- タイトル設定画面の操作ヒント表示切替、ウィンドウ／フルスクリーン切替、戻る操作
- `canvas_items`／`keep`／`fractional` による、16:9 を維持したウィンドウ拡縮
- プレイヤーの左右移動、重力、ジャンプ
- idle / walk のスプライトアニメーションと左右反転
- プレイヤー追従用 `Camera2D`
- Terminal／Scene Exit の近接検知と `E` キー操作
- 端末認証によるドア解放アニメーション
- 閉鎖中の Door による物理的な通行阻止と、開放後の当たり判定無効化
- Scene Exit による `prototype_room_a.tscn` と `prototype_room_b.tscn` の往復
- `GameState.world_flags` による端末・ドア状態の実行中保持
- `next_spawn_id` と共通 Room Controller による部屋ごとのスポーン適用
- Room Controller の Inspector 設定による部屋別 Camera 境界
- テキストボックス、操作ヒント、フェードアウト
- 複数の近接範囲に対応する、発生元ごとの操作ヒント管理
- ポーズ／再開、設定画面の表示
- `GameState.interaction_hints_enabled` による、タイトル画面とゲーム内 UI で共通の操作ヒント表示 ON/OFF
- 操作ヒント表示設定の現在の実行中におけるシーン間保持
- ポーズメニューからタイトルへの復帰

## 実装済み UI のうち機能が未接続のもの

- BGM 音量スライダーは変更値をコンソールへ出力するのみで、音量ミキサーには接続されていない。
- 言語選択は選択値をコンソールへ出力するのみで、ローカライズ処理には接続されていない。
- 設定内容はセッションをまたいで保存されない。
- BGM と言語のラベルには、現在未接続であることを表示している。

## 未着手の会話 UI・キー操作

- 正式な会話ボックスは未実装である。現在のテキストボックスは、調査・認証・移動制限などの短いフィードバック表示に使用する簡易 UI であり、正式な会話システムではない。
- 会話の送り、選択、キャンセル、会話中のプレイヤー操作制限に使用するキー構成は未確定・未実装である。
- 正式な会話 UI とキー構成は、第一休眠室の制作時に必要な仕様を確認してから実装する。現段階では既存の `interact`（E）、`pause`（Esc）、Godot 標準 UI 操作を変更しない。

## Demo 開発範囲

完成 Demo の制作範囲は、最大で第二章「下水道」までとする。世界観、主人公、開示順、調査システムの設計は各設計文書に記録済みであり、今後は実装対象へ落とし込む段階にある。

## 2026 年 7 月末・前期発表の目標

- 灰盒と方形ブロックを用いて、序章と第一章の概略シーンを構築する。
- 序章と第一章の完全な進行フローを作る。
- シーン間の接続とマップ相互接続を実装する。
- 調査システム、主要な謎解き・仕掛け、シーン状態の記録を実装する。
- 正式な美術の完成を必須条件とせず、実際に遊べる灰盒 Demo を完成させる。

## 次の開発計画

1. 正式な第一シーンである序章 Room 1「Cryogenic Room（休眠ポッド室）」の制作を開始する。現在の START 遷移先 `prototype_room_a.tscn` は開発確認用の一時的な対象であり、正式な開場シーンではない。
2. 物体名表示、詳細表示型、簡易表示型、詳細表示中の一時停止から成る調査システムを実装する。
3. 第一章「貧民区」のマップ、調査対象、謎解き、序章との接続を実装する。
4. 複数シーンの進行に必要な状態管理を設計・実装する。
5. 7 月末の発表範囲外である第二章「下水道」以降の詳細は、必要に応じて作業を進める。具体的な優先順位は TBD。
